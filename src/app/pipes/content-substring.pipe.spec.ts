import { ContentSubstringPipe } from './content-substring.pipe';

describe('ContentSubstringPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentSubstringPipe();
    expect(pipe).toBeTruthy();
  });
});
